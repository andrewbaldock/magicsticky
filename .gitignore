# personal data — the dogfood list + future local store (never publish)
TODO.md
store.json
data/
.env
.env.*
!.env.example
# Phase 2 SQLite store + WAL sidecars (local/Fly-volume data — never publish)
*.db
*.db-shm
*.db-wal

# bun / node
node_modules/
*.log

# build + test output (artifacts, never committed)
web/dist/
test-results/
playwright-report/

# scratch: out-of-band Claude Code <-> Cowork conversation, not project artifacts
CONVO-CC*.txt
CONVO-CW*.txt
